'use strict';

//const Bowling = require('./bowling');

require('should');

xdescribe('Bowling', () =>{
  //let game = new Bowling();

  it('should do something', () => {
    'the answer'.should.equal(42);
  });
});
